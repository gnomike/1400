a = float(input("Введите число a (1 < a ≤ 1.5): "))

n = 2  # Начинаем с n = 2 (первое число: 1 + 1/2 = 1.5)

while True:
    current = 1 + 1 / n
    if current < a:  # Если число стало меньше a, выходим из цикла
        break
    print(f"{current:.6f} (1 + 1/{n})")
    n += 1
