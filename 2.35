# Ввод данных
V1 = float(input("Введите скорость первого автомобиля (км/ч): "))
V2 = float(input("Введите скорость второго автомобиля (км/ч): "))
S = float(input("Введите начальное расстояние между автомобилями (км): "))

# Проверка корректности входных данных
if V1 <= V2 or S <= 0:
    print("Ошибка: скорость первого автомобиля должна быть больше второго, а расстояние положительным!")
else:
    # Вычисление относительной скорости
    relative_speed = V1 - V2
    
    # Время в часах
    time = 0.5  # 30 минут
    
    # Расчет итогового расстояния
    final_distance = S + relative_speed * time
    
    print(f"\nЧерез 30 минут расстояние между автомобилями будет: {final_distance:.2f} км")
