import math

a = float(input("Введите число a (1 < a ≤ 1.5): "))

# Вычисляем максимальное n, для которого 1 + 1/n >= a
max_n = math.floor(1 / (a - 1))

# Печатаем все n от 2 до max_n включительно
print("Значения n, при которых 1 + 1/n >= a:")
for n in range(2, max_n + 1):
    print(n)
