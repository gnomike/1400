import math

a = float(input("Введите число a (1 < a ≤ 1.5): "))

# Вычисляем искомое n по формуле
n = math.floor(1 / (a - 1)) + 1
result = 1 + 1 / n

print(f"Наименьшее n: {n}")
print(f"Соответствующее число: {result} (1 + 1/{n})")
