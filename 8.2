n = int(input("Введите число n: "))

i = 1  # Начальное значение натурального числа

# Цикл с условием: пока квадрат числа не больше n, увеличиваем i
while i * i <= n:
    i += 1

# После выхода из цикла i * i будет первым числом, большим n
print(f"Первое число, большее {n}: {i * i}")
