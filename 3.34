# Перебираем все трёхзначные числа
for x in range(100, 1000):
    a = x // 100           # первая цифра (сотни)
    remainder = x % 100    # оставшиеся две цифры (десятки и единицы)
    result = remainder * 10 + a
    if result == 564:
        print("Искомое число x:", x)
        break
